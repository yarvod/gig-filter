from interface.windows.graphWindow import GraphWindow


class StabilityMeasureGraphWindow(GraphWindow):
    window_title = "Stability Measure graph"
    graph_title = "Power (time)"
    y_label = "Power, dBm"
    x_label = "Time, s"
